import 'package:flutter/material.dart';

class AppColors {
  static const background = Color(0xFF0F1012);
  static const backgroundSecondary = Color(0xFF15181C);
  static const surface = Color(0xFF1A1D21);
  static const surfaceSoft = Color(0xFF20252B);

  static const textPrimary = Colors.white;
  static const textSecondary = Colors.white70;
  static const border = Colors.white10;

  static const primary = Colors.white;
  static const onPrimary = Colors.black;

  static const success = Colors.green;
  static const warning = Colors.orange;
  static const danger = Colors.red;
  static const info = Colors.blue;
}
